/**
 * @flatcraft/types — спільні Zod-схеми та DTO між web / api / worker.
 *
 * Phase 0.1: лише placeholder, щоб монорепо було узгодженим і компілювалось.
 * Реальні схеми (api/, domain/, templates/, jobs/) додаються у Phase 1+.
 * Див. docs/02_ROADMAP.md і AGENTS.md.
 */

export const TYPES_PACKAGE_VERSION = "0.0.0" as const;
