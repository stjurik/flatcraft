/**
 * @flatcraft/db — Drizzle ORM schema, клієнт, міграції, seed.
 *
 * Phase 0.1: placeholder. Схема (schema.ts), клієнт (client.ts), seed (seed.ts),
 * migrate (migrate.ts) додаються у Phase 0.3. Див. docs/05_DATA_MODEL.md.
 */

export const DB_PACKAGE_VERSION = "0.0.0" as const;
