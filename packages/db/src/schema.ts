/**
 * Drizzle schema — єдине джерело істини для структури БД.
 *
 * Кожне нетривіальне рішення (типи UUID, soft-delete, JSONB-валідація)
 * задокументовано в docs/05_DATA_MODEL.md і відповідних ADR.
 *
 * Колонки та індекси визначаються у наступних коммітах цього PR
 * (TDD: спочатку тести на форму схеми у schema.test.ts).
 */

export {};
