node_modules
.next
.turbo
dist
build
coverage
**/*.lock
pnpm-lock.yaml
uv.lock
workers/cad/
*.dxf
*.step

# Drizzle-kit generated metadata — не чіпаємо вручну і не reformat'имо.
packages/db/src/migrations/

# Baked bend-matrix — згенеровано з YAML (Hotfix 2.9.c, ADR-022).
packages/cad-engine/src/generated/baked-spec.ts

# Discord snapshot — згенеровано `pnpm discord:snapshot` (ADR-023). Формат
# визначає скрипт (lib/markdown.ts), не prettier — інакше weekly Action
# і скрипт дають різний формат → фальшивий drift. ONBOARDING.md не чіпаємо
# (ручний). Лише два генеровані артефакти:
docs/discord-config/snapshot.json
docs/discord-config/README.md
