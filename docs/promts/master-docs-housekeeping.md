[MASTER RUN 4 — docs housekeeping: синхронізація док-бази з фактичним станом]

МЕТА
Усунути розсинхрони док-бази після швидких фаз 3.3-3.5: відновити ADR-027,
додати відсутні Roadmap-секції, закрити борг progress-log/§13 по Phase 3.4,
оновити статуси. БЕЗ зміни моделі Roadmap (треки/ритми — окреме рішення,
НЕ в цьому run'і) і БЕЗ нових архітектурних рішень.

РЕЖИМ АВТОНОМІЇ (патерн docs/16; БАЗА ГІЛКИ — перший крок)

- Гілка docs/housekeeping-2026-07 від свіжого origin/main (merge-base guard).
- ЗОЛОТЕ ПРАВИЛО RUN'У: verify-then-write. КОЖНА правка спирається на
  git-факт (номер PR, SHA, дата merge) — перевіряй через gh/git log перед
  написанням. Нічого не вигадуй; дата невідома → напиши «дата не
  зафіксована» замість вигаданої.
- ≥2 варіанти → «Опитування» у PR description (★рекомендація + дефолт).
- docs-only: правки ЛИШЕ у docs/ (+ PR description). CLAUDE.md НЕ чіпати —
  для нього готуєш погоджений текст (див. WP5).
- Один draft PR, план у description, «Як перевірити очима» = список
  тверджень для вибіркової перевірки. НІКОЛИ не мержиш.

КОНТЕКСТ
CLAUDE.md (§6, §12, §13), docs/02, docs/03, docs/04 (R-01), docs/12,
docs/13, docs/14, docs/15, docs/00; gh pr list --state merged (номери
#54-#73), git log по docs/03_DECISIONS.md.

──────────────────────────────────────────────
WP1 — Аудит (перший коміт: docs/promts/inputs/housekeeping-audit.md)
Звір фактичний стан і зафіксуй таблицею (це вхід для решти WP):

1. Долі PR #69 (QR-фідбек) і follow-up issue #70: merged? яким PR закритий
   #70? Дати, SHA.
2. ADR-027: git log --all --oneline -- docs/03_DECISIONS.md +
   git log -S "ADR-027" — чи існував текст ADR-027 в історії (Phase 3.0
   PR 1 мав його створити), і в якому коміті зник.
3. Список OQ у docs/00, які фактично вирішені пізнішими ADR (перевір
   кожен проти docs/03/13; кандидати: OQ-9 перфорація/ADR-031 — але
   НЕ закривай нічого без прямого посилання на рішення).

──────────────────────────────────────────────
WP2 — docs/03_DECISIONS.md
A. Відновити ADR-027 «Products як preset базового шаблону»:

- якщо WP1 знайшов текст в історії → відновити ДОСЛІВНО з приміткою
  «відновлено після втрати при merge, оригінал <SHA>»;
- якщо тексту ніколи не було → реконструювати з docs/13 (запис Phase 3.0)
  і docs/promts/phase-3-0-pr1-architecture.md, з явною позначкою
  «реконструйовано ретроспективно YYYY-MM-DD; першоджерело — progress log».
  B. Зміст-індекс зверху файлу: таблиця (№ / назва / статус / дата) усіх ADR,
  включно з Superseded. Далі індекс підтримується кожним новим ADR.

──────────────────────────────────────────────
WP3 — docs/02_ROADMAP.md (мінімальний порядок, БЕЗ зміни моделі)
A. Легенда зверху: пояснення двох систем нумерації («Phase 0-5 — історичний
MVP-план; Phase 3.x — Architecture Evolution track (docs/14); історичні
Phase 3 Auth / Phase 4 Donations відкладені за ADR-020 і НЕ пов'язані
з 3.x»).
B. Додати відсутні секції-резюме (по 3-6 рядків + посилання на повний
запис у docs/13): Phase 3.0 Products Catalog, 3.1, 3.2, 3.4 QR-фідбек
(статус з WP1). Розмістити хронологічно поряд з 3.3/3.5.
C. Звірити чекбокси 3.3/3.5/5.x з фактом (нічого не «закривати» без PR-факту).

──────────────────────────────────────────────
WP4 — docs/13 + борг закриття Phase 3.4
Якщо WP1 підтвердив merge #69 (+#70-follow-up): повний запис «Feature 3.4 —
QR-фідбек з виробництва» нагору docs/13 за форматом попередніх (PR, рішення
Q1-Q5 з опитування, інваріанти: 404 на невідомий export_id, rate-limit,
подія feedback_submitted, digest-секція deviation; снапшоти QR — свідомий
regen у follow-up). Якщо follow-up НЕ merged — запис із чесним статусом
«частково: форма є, QR-URL у PDF ще старий».

──────────────────────────────────────────────
WP5 — дрібні синхронізації
A. docs/04 R-01: mitigation 4 → реалізовано (дата, PR) — якщо WP1 підтвердив.
B. docs/15: позначити A2/B3 виконаними (дата, PR/ADR) у преамбулі відповідних
промптів — промпти НЕ видаляти (бібліотека).
C. docs/00: закрити ЛИШЕ ті OQ, для яких WP1 знайшов пряме рішення
(перенести відповідь у docs/01 за конвенцією файлу).
D. У PR description — розділ «РУЧНІ ПРАВКИ CLAUDE.md ДЛЯ YURII» з готовим
текстом двох правок (CLAUDE.md тобі заборонений deny-правилом):

1.  §12 додати посилання: docs/11_OBSERVABILITY, docs/12_TEMPLATE_CONTRACT,
    docs/14_ARCHITECTURE_EVOLUTION, docs/15_LLM_PROMPTS,
    docs/16_AUTONOMOUS_RUNS (+docs/17, якщо існує);
2.  §13 ротація під Feature 3.4 (якщо WP4 створив запис): снапшот +
    milestones (3.4, 3.3, 3.2 — видалити 3.1) + footer-дата.
    G. Цей файл (docs/promts/master-docs-housekeeping.md) — включити у PR.

ТЕСТИ
prettier/lefthook по md зелені; жодних правок поза docs/.

ФІНАЛ
Таблиця: знахідка → правка → git-факт-підстава. Опитування (якщо виникло).
Чеклист yurii: merge PR → 2 ручні правки CLAUDE.md → commit
"docs: sync CLAUDE.md §12-13 (housekeeping)". STOP.
