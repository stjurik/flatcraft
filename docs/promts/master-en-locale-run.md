[MASTER RUN 8 — EN-версія, етап A: i18n-фундамент + маркетингові поверхні.
Мультиагентний конвеєр Claude+agy: Розвідник → Архітектор+Будівельник → Рев'юер]

ПРИЗНАЧЕННЯ
Перший запуск конвеєра ролей ADR-036 з живим Gemini (локальний агент `agy`,
див. docs/promts/agy-orchestration-recommendations.md). Телеметрія показала
~50% англомовних користувачів → EN-версія обґрунтована даними (закриває
OQ-19). Етап A СВІДОМО не чіпає студії/форми/валідацію — вони підуть
етапом B ПІСЛЯ Registry (Run 7), інакше робота згорить у міграції.

ПРАВИЛА ВИКОРИСТАННЯ agy (ОБОВ'ЯЗКОВІ, пріоритет над рекомендаціями agy)

- agy викликається ЛИШЕ headless: `agy -p "..."` (ніколи інтерактивно).
- agy — READ-ONLY щодо коду: результати пише ТІЛЬКИ у docs/promts/inputs/.
  ЗАБОРОНЕНО: `--dangerously-skip-permissions`, правки agy поза inputs/,
  фонові суб-агенти agy. Порушення = STOP.
- Файлові гонки: після виклику agy дочекайся завершення процесу, потім
  читай/правь файли сам. Не редагуй те, що agy зараз пише.
- Таймаут agy ~5 хв: ріж делеговані завдання на шматки (по директорії /
  по темі), а не «проскануй усе».
- Кожен вихід agy — ВЕРИФІКУЙ проти коду перед використанням
  (verify-then-write; agy може помилятись так само, як будь-яка модель).

СПІЛЬНІ ПРАВИЛА СТАДІЙ (патерн docs/16)
Гілки від origin/main (merge-base guard); force-push заборонено;
≥2 варіанти → «Опитування» (★ + дефолт-при-merge); PR = draft, МЕРЖИТЬ
YURII (без делегування — тест конвеєра включає його гейт); «Як перевірити
очима» обов'язковий; оновлювати issue «Черга yurii» (#78) після стадії.

═══════ СТАДІЯ 1 — РОЗВІДНИК (Claude Haiku/Sonnet — оркеструє, agy — сканує) ═══════
Гілка docs/i18n-inventory, docs-only draft PR.

1. Делегуй agy сканування ПОШМАТКОВО (Паттерн 2 з рекомендацій; по одному
   виклику на зону, результат у docs/promts/inputs/i18n-scan-<зона>.md):
   зона 1 — лендінг + /about + /soon; зона 2 — каталог /templates + картки
   - states + Footer/SiteLinks; зона 3 — export-flow UI (ExportButton,
     PostExportDonateNudge) + OG/layout-метадані; зона 4 — /privacy + /terms
     (вже двомовні — зафіксувати їхній патерн як прецедент).
2. ВЕРИФІКУЙ вибірково кожен скан проти коду (мін. 5 рядків на зону),
   зведи у docs/promts/inputs/i18n-inventory.md: файл | рядок | UA-текст |
   тип (static/dynamic/DB) + перелік готових DB-полів локалізації
   (name_uk/name_en) + ОКРЕМО список «НЕ входить в етап A» (студії,
   AutoForm .describe()-лейбли, Zod-повідомлення, PDF).
3. У той самий PR: включи untracked docs/promts/agy-orchestration-
   recommendations.md і цей файл (конвенція docs/promts); docs/15 §0 —
   рядок Reviewer/Scout: статус Gemini «pending» → «активний (agy,
   2026-07-20)». Розбіжності скан-vs-код занотуй у PR (перша метрика
   якості agy).

═══════ СТАДІЯ 2 — АРХІТЕКТОР+БУДІВЕЛЬНИК (Claude сильна) ═══════
Вхід: змержений PR Стадії 1.
A. Дозволено ОДИН research-виклик agy: search_web по актуальному стану
next-intl для Next.js 15 App Router (версія, відомі issues) →
inputs/i18n-research.md; використовуй як довідку, рішення приймаєш сам.
B. ADR-037 «i18n-архітектура» у docs/03 (+індекс), рішення
(ALT/CHOICE/RATIONALE/CONSEQUENCES):

1.  механізм: next-intl vs власні словники (App Router dictionary
    pattern, нуль deps) vs paraglide — згода yurii на нову top-level
    залежність Є, якщо ADR її обере (§6);
2.  routing: uk без префікса (дефолт) + /en/\*; hreflang, sitemap,
    og:locale;
3.  визначення мови: Accept-Language перший візит + перемикач у
    header/footer + функціональна cookie вибору (узгодити текст
    у /privacy);
4.  словники apps/web/src/i18n/{uk,en}.json, типізовані ключі +
    DB-поля для сутностей;
5.  межа A/B: список зі Стадії 1; етап B після Registry; Consequence:
    TemplateDefinition (Run 7) має передбачити локалізовані
    label-метадані — занести опитуванням у Run 7;
6.  мова PDF/креслень — відкладено (ISO-креслення значною мірою
    мовонезалежні) → «Опитування».
    C. Імплементація етапу A за ADR-037 та inventory: фундамент + переклад
    усіх поверхонь Стадії 1; EN-тексти — природна інженерна англійська
    для DIY/maker-аудиторії, не дослівний переклад; перемикач мови (44px);
    e2e: обидві локалі рендеряться, перемикач працює, console-clean,
    existing e2e зелені (uk-URL незмінні).
    D. Гілка feat/i18n-stage-a → draft PR: план, «Опитування», «Як перевірити
    очима» (відкрити /en, пройти лендінг→каталог→export happy-path EN,
    перемкнути мову туди-назад, 6 тверджень про тексти). НЕ мержити.

═══════ СТАДІЯ 3 — РЕВ'ЮЕР (ОКРЕМА свіжа Claude-сесія — оркеструє, agy — рев'юїть) ═══════
Вхід: draft PR Стадії 2. Ця сесія НЕ править код — лише організовує рев'ю.

1. Основне рев'ю — agy (Паттерн 1), пошматково через таймаут:
   `gh pr diff <N> -- <зона> | agy -p "Ти незалежний Рев'юер проєкту
flatcraft (інваріанти: CLAUDE.md §7, ADR-037). Перевір: відповідність
ADR-037, повнота проти docs/promts/inputs/i18n-inventory.md, якість
EN-копірайту 10 ключових рядків (природність, tone maker-аудиторії),
відсутність змін uk-URL і workers/. Вердикт ✅/⚠️/❌ + список →
docs/promts/inputs/review-i18n-a-<зона>.md"`.
2. Опублікуй вердикти agy ДОСЛІВНО коментарем у PR з позначкою
   «Gemini Reviewer (unedited)». Свою думку — ОКРЕМИМ коментарем лише
   якщо бачиш фактичну помилку у вердикті agy (розбіжність двох моделей —
   цінний сигнал, обидві позиції лишаються видимими yurii).
3. Онови issue #78: «PR готовий до рев'ю yurii; вердикт Gemini: <✅/⚠️/❌>».
   Merge — yurii після прочитання обох вердиктів.

ФІНАЛ КОЖНОЇ СТАДІЇ: короткий звіт + STOP. Наступну стадію запускає yurii —
це і є тест конвеєра з людськими гейтами.
