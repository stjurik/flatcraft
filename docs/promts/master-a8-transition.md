# MASTER RUN — перехід на A8: контейнер агента, демон-оркестратор, прев'ю

> **Що це.** Промпт, на який посилаються ADR-039, `AGENTS.md` §A8 і `docs/19` §6. Розгортає
> середовище автономної розробки на вже побудованій машині A8.
>
> **Роль:** Архітектор → Будівельник (`docs/15` §0). **Модель:** сильна (Opus).
>
> **Режим — ІНТЕРАКТИВНИЙ, не headless.** Прогін пише в `infra/**`, а це незворотний шлях
> (інваріант `docs/19` §7.3). Крім того `.claude/settings.autonomous.json` механічно забороняє
> `Edit(infra/**)` — запуск через `tools/scripts/autorun.sh` впаде за побудовою, і це правильно.
> yurii присутній на гейті кожної стадії.
>
> **Гілка:** нова від свіжого `origin/main`, окрема на кожну стадію. Merge-base guard —
> `docs/16` §3 крок 0.

---

## 0. Де запускати і що читати

**Запускай у `~/hart` — репозиторії `flatcraft`.** Деліверабл прогону — код у
`infra/ansible/roles/a8/`, а контракт (CLAUDE.md, ADR-039, інваріанти, стиль guard-скриптів)
живе там само. Сесія, відкрита в теці A8-репозиторію, цього не бачить і прогін виконати не
може — це не питання дозволів, а питання того, що лежить на диску поруч.

Прочитай:

- `CLAUDE.md` (весь), `AGENTS.md` §A8
- `docs/03_DECISIONS.md` — **ADR-039 повністю** (це контракт середовища), ADR-040 §1, ADR-011
- `docs/19_A8_PREFLIGHT.md` §6 і §7
- `docs/02_ROADMAP.md` трек T5, кроки 5–8 (там числові критерії виходу)
- `docs/16_AUTONOMOUS_RUNS.md` §1 і §3

**Якщо запуск усе ж поза `flatcraft`** — репозиторій публічний, тож файли тягнуться без
жодної авторизації:

```bash
mkdir -p /tmp/hart-ref && cd /tmp/hart-ref
B=claude/agent-orchestration-planning-w06nh9
R=https://raw.githubusercontent.com/stjurik/flatcraft/$B
for f in CLAUDE.md AGENTS.md docs/03_DECISIONS.md docs/19_A8_PREFLIGHT.md \
         docs/02_ROADMAP.md docs/16_AUTONOMOUS_RUNS.md \
         docs/promts/agy-orchestration-recommendations.md \
         tools/scripts/check-forbidden-paths.sh; do
  curl -fsSL "$R/$f" -o "$(echo "$f" | tr / _)" || echo "НЕ ЗАВАНТАЖИВСЯ: $f"
done
ls -la
```

⚠ Цей запасний шлях працює саме тому, що `flatcraft` **публічний**. Після D.1 знадобиться
токен — ще один рядок у бюджет рішення про visibility (§7 п.1).

**Стан A8.** Факти в §1 нижче самодостатні — для старту читати нічого не треба. Їхнє
джерело — `PROGRESS.md` з приватного репозиторію **`stjurik/a8-ops`** (локально —
`~/projects/a8-remote`). Розділення навмисне: `flatcraft` публічний, а ті документи містять адреси
каналів доступу, правила фаєрвола, топологію tailnet і відбитки ключів — класифікація
даних робиться межею репозиторію, а не редагуванням тексту.

Тому **не копіюй звідти нічого дослівно у `flatcraft`.** Потрібна деталь поза §1 —
прочитай у тому репозиторії й перекажи фактом, без адрес і відбитків.

**Мінімальний контракт, який діє навіть якщо жоден файл не прочитано, — §2 нижче.**

## 1. Стан на момент старту — не перевідкривай

**Машина побудована.** За `PROGRESS.md` (10.09) Checkpoints A, B, C зелені: Ubuntu 24.04.4,
ядро 6.8.0-139, Docker 29.8.0, Node v22.23.2, pnpm 12.3.4, uv 0.12.10, Playwright,
користувачі `yurii`/`runner`/`agent`, UFW active, `DOCKER-USER` з автовідновленням.
**Автономність після знеструмлення підтверджена емпірично** — 8 год 55 хв, машина піднялась сама.

**Канали доступу:** `a8-lan` (`192.168.1.20` через `ProxyJump owrt-ts`) — робочий, ним і
працюй; `a8-ts` (`100.110.195.49`) — індикатор. Прямий `a8` — won't fix.

**Уже в git, не переробляй** (коміт `5e11cf6`): `.claude/settings.autonomous.json` +
`autorun.sh --settings`; `prove-red-before-green.sh`; `check-deny-parity.sh`; job `guards`
у `ci.yml`; toolchain і Postgres у `ai-fix.yml`.

**Стадія 0 виконана 2026-09-15** (PR #109, коміти `fbd36b2`, `84fc94f`; `a8-ops`
`c68fdb2`, `ec314ed`). Обидва пули на A8 працездатні під користувачем `agent`:

- **Gemini:** зонд `check-agy-headless.sh` → exit 0, nonce дослівно, `scope: OK`. Спрацював
  варіант 2 `docs/19` §D.4 — копіювання `~/.gemini` з T470. Креденшал **спільний** для двох
  машин: перелогін на T470 ламає A8;
- **Claude:** `claude -p` → exit 0, без `401`. Токен дійсний **1 рік** від 2026-09-15, живе
  у **двох** місцях (A8 і GitHub Secret), бо `ai-fix.yml` лишається запасним шляхом.
  Поновлення мусить покривати обидва — інакше інцидент `401` відтворюється.

## 2. Явна авторизація і межі

`CLAUDE.md` §6 забороняє писати в `infra/ansible/` без явного завдання. **Це завдання —
явне, і воно розблоковує рівно один новий шлях:** `infra/ansible/roles/a8/` з усім вмістом.

Усе інше з переліку заборонених шляхів лишається закритим: `packages/db/src/migrations/**`,
решта `infra/**` поза новою роллю, `.github/**`, `CLAUDE.md`, `docs/03_DECISIONS.md`,
`docs/12_TEMPLATE_CONTRACT.md`, `packages/db/src/schema.ts`, `workers/cad/tests/snapshots/**`,
`packages/cad-engine/data/bend-machine-esi.yaml`.

Інваріанти, які прогін не має зламати (`docs/19` §7, `PROGRESS.md` §1.1):

1. **На A8 немає prod-креденшалів** — ні пароля vault, ні SSH-ключа до Mirohost, ні
   GHCR-токена на запис. `deploy-staging.yml` і `release.yml` лишаються на GitHub-hosted.
2. **Жодного вхідного порту з інтернету.** Прев'ю публікують порти **тільки на `127.0.0.1`**,
   назовні їх віддає `cloudflared` вихідним з'єднанням.
3. Незворотне — за yurii.
4. Deny-правила портативні.
5. **Kill switch і денний ліміт існують ДО першого автономного прогону**, не після.
6. Self-hosted runner на публічному репо — ніколи (цей прогін раннера не заводить, див. §6).

## 3. Стадія 1 — контейнер агента + демон-оркестратор + запобіжники

Це головна стадія: саме вона дає те, заради чого будувалась машина.

**Контейнер агента** (ADR-039 §2): образ із Claude Code і `agy`; запуск під користувачем
`agent` (без sudo); **власний git-worktree на задачу**; allowlist вихідної мережі;
`--dangerously-skip-permissions` дозволений **ЛИШЕ всередині контейнера** і заборонений на
хості.

⚠ **Справжній блокер — довіра Claude Code до робочої теки на A8.** Дослівно з прогону
2026-09-15:

```
Ignoring 37 permissions.allow entries from .claude/settings.json: this workspace has not
been trusted. Run Claude Code interactively here once and accept the trust dialog, or set
projects["/home/agent/hart"].hasTrustDialogAccepted: true in /home/agent/.claude.json
```

Тобто агент, який «має» 37 записів `permissions.allow`, фактично їх не має — і повідомляє
про це одним рядком, який легко проґавити. У headless-режимі це означає, що інструменти,
яким потрібен явний allow, будуть відхилені, і прогін не зробить роботу.

**Виміряно окремо (Claude Code 2.1.272, тека, яку ніколи не довіряли, з контролем):**
`deny`-правила з файлу, переданого через `--settings`, **діють і в недовіреній теці** —
контрольний прогін без `deny` файл створив, прогін із `deny` не створив. Тобто
`.claude/settings.autonomous.json` захищає навіть тут; «жодних механічних обмежень» —
сценарій, який **не** справдився.

**Що досі НЕ виміряно — і що робиться ОКРЕМИМ прогоном ДО цієї стадії:** чотири виміри, серед
них — чи переживає `deny` прапорець `--dangerously-skip-permissions`, який ADR-039 §2 наказує
вмикати **всередині контейнера** (у хмарній сесії не перевіряється: під root прапорець
відмовляється працювати). Промпт — **`docs/promts/a8-stage-1-step-0-measurements.md`**.

Не починай цю стадію, доки його звіт не прийнято. Якщо `deny` не переживає прапорець, безпека
контейнера тримається виключно на ізоляції (без sudo, власний worktree, allowlist мережі), і це
має бути записано фактом, а не припущене — від цього залежить, що саме ти будуєш.

**Ще один факт із того ж виміру:** у `deny`-списку працюють лише `Edit(...)`; `Write(...)`
для файлових перевірок **інертні** (дослівно: «only `Edit(path)` rules are ... Edit rules
cover all file-editing tools»). У `.claude/settings.autonomous.json` 9 діючих `Edit` і
9 інертних `Write`, у кожного є пара — **захист не має дірки**, але половина списку
декоративна. Небезпека не в наявних рядках, а в патерні: хто додасть нове правило лише як
`Write(шлях)`, не отримає нічого.

**Демон-оркестратор** (ADR-039 §3): systemd-юніт; черга задач на диску; **свіжа сесія на
задачу**; стан **ніколи не живе в контексті моделі**. Демон сам по собі без LLM — це
супервізор, а не агент.

**Класифікатор виходу** (ADR-039 §4) — три класи:

- **Authentication** — дослівна сигнатура `401` → **STOP усієї черги**;
- **Quota** — **сигнатури НЕМАЄ.** ADR-039 свідомо її не вигадує. Твоє завдання —
  **виміряти** її на реальному вичерпанні, а не написати регулярку з голови. Доки не
  виміряна — цей клас падає в «Other»;
- **Other** — три failed поспіль → пауза черги.

**Запобіжники** (ADR-039 §7): kill-switch файл; денний ліміт задач; журнал прогонів веде
**супервізор, а не агент** (агент не пише сам про себе); пауза після трьох відмов.

**Протокол питань** (ADR-039 §6): клас A (незворотне, без дефолту, чекає безстроково) і
клас B (оборотне, ★рекомендація + дефолт, таймер 6–12 год); шість обов'язкових полів,
перевіряються механічно; бюджет — не більше 3 питань на задачу і 10 на добу. Канали Discord
заводяться через `infra/discord/` (ADR-023), `pnpm discord:apply` — **manual-only, запускає
yurii**.

**Паралельність демона визнач вимірюванням, не припущенням:** зніми `free -m` на A8 і
порахуй від фактичної RAM за пікове споживання агентської сесії. Число і розрахунок — у PR.

**Критерій виходу (з `docs/02` T5 крок 5, числовий):**

- демон піднімає тестову задачу end-to-end (черга → worktree → сесія → draft PR)
  **3 рази поспіль без ручного втручання**;
- kill switch зупиняє чергу **≤ 60 с**;
- машина після примусового ребуту повертається у робочий стан **≤ 5 хв** без участі людини.

Не «виглядає працюючим» — саме ці три числа, з дослівним виводом у PR.

## 4. Стадія 2 — прев'ю-середовища на гілку

Це те, що прямо здешевлює приймання yurii — вузьке місце всього конвеєра.

- `docker compose` per-branch + маршрут у Cloudflare Tunnel (`a8-preview`, D.5).
- **Порти тільки `127.0.0.1:`** — інваріант 2. Перевірка: `ss -ltnp` показує `127.0.0.1:PORT`,
  не `0.0.0.0:PORT`.
- Диск на A8 — 226 GB (нижче за «мінімум робочий 250 GB» з `docs/19` §1; yurii вирішив
  нарощувати за потреби). Тому три умови проєктування, а не блокер: **ліміт одночасних
  прев'ю**, **автоприбирання після закриття PR**, **алерт на заповнення диска**.
- Посилання на прев'ю — у тіло draft PR, поряд із «Як перевірити очима».

**Критерій виходу:** yurii відкриває URL із PR у браузері й бачить гілку; після закриття PR
стек зникає, місце звільнене (перевірено `df -h` до і після).

## 5. Стадія 3 — Ansible-роль `a8` + бекапи

Страховка: машина зібрана руками, і сьогодні існує лише як стан диска.

- `infra/ansible/roles/a8/` — Track B–C як код (користувачі, sshd, UFW, Docker, toolchain,
  swap, logind, GRUB, journald), щоб A8 перевстановлювалась за годину. Джерело істини для
  фактичних значень — `PROGRESS.md` §1 і `A8_UBUNTU_INSTALL.md`, **не пам'ять**. Обидва лежать
  поза git — до початку цієї стадії вони мають опинитися в репозиторії (§7 п.3), інакше роль
  писатиметься зі слів, а не з джерела.
- **Роль має бути ідемпотентною:** другий прогін дає нуль `changed`. Це і є її тест.
- Бекап-роль: щоденний `git clone --mirror` + копія `pg_dump` з R2 (компенсація R-07 —
  ADR-038 послаблює mitigation 1 «Open Source MIT → community підхопить»).
- Healthcheck-cron A8 → Discord `#інциденти`.

**Критерій виходу:** `ansible-playbook … --check` на живій A8 — нуль `changed`; повторний
реальний прогін теж нуль.

## 6. Чого цей прогін НЕ робить

- **Не заводить self-hosted runner (D.2) і не переводить репо в private (D.1).** Виміряно:
  CI коштує 16.3 білінгових хв/PR, безкоштовний ліміт приватного репо — 2 000 хв/міс, тобто
  ~123 PR/міс проти історичного піку 38. Раннер економить **$0** сьогодні і ≈$17/міс при
  вдесятеро більшій швидкості, а коштує трьох умов `PROGRESS.md` §6.5 плюс постійної
  дисципліни ланцюга постачання. ADR-039 у власній назві розділяє «оракули в CI, агенти на
  залізі» — цей прогін реалізує саме її. Рішення лишається за yurii, див. §7.
- **Не робить auto-merge (ADR-040).** Це крок 7 треку T5, після кроку 6 «автономія під
  наглядом». Порядок не переставляти.
- **Не піднімає `NODE_VERSION` 20 → 22** — окремий пункт черги T1, разом із залежностями.
- **Не чіпає workflow-файли** у `.github/` — і не потребує: без раннера правок там немає.
- **Не запускає `pnpm discord:apply`** — manual-only (CLAUDE.md §6).

## 7. «Опитування» — винести в PR кожної стадії

**Повний розбір варіантів, наслідків і ★рекомендацій — `docs/20_OPEN_DECISIONS.md`.** Нижче
лише те, що стосується безпосередньо цього прогону; номери `OQ-N` — звідти.

1. **Доля D.1/D.2 — `OQ-30`.** Аргумент за скасування у §6, числа й розбір — у `docs/20`.
   Коротко: раннер не економить сам по собі, він повертає гроші, які забирає private, а
   бізнес-драйвер ADR-038 (комерційне клонування) від раннера не залежить. ★ скасувати D.2,
   D.1 лишити окремим бізнес-рішенням.
2. **Довіра робочої теки — `OQ-22` (Claude Code, клас A, блокує цю стадію)** і
   `trustedWorkspaces` для worktree (`agy`). Друге закрите PR #109:
   `tools/scripts/trust-worktree.sh` реєструє корінь worktree, `autorun.sh` кличе його після
   `worktree add`, вердикт 7 знято. Перше — відкрите; бери `trust-worktree.sh` за зразок.
3. **Документи A8 — рішення ухвалене 2026-09-14: приватний репозиторій `stjurik/a8-ops`**
   (локально `~/projects/a8-remote`), 30 файлів, перенесення виконав yurii. Розділення
   навмисне — див. §0. Варіант «перенести у `flatcraft` як `docs/20`/`docs/21`» відхилено;
   ці номери вільні й зайняті іншим. Лишається живим тільки одне: до початку Стадії 3
   переконайся, що `PROGRESS.md` і `A8_UBUNTU_INSTALL.md` у тому репозиторії **закомічені й
   запушені**, а не лежать на диску — інакше роль писатиметься зі слів.
4. **Інваріант 6 у `docs/19` §7 — `OQ-32`.** `PROGRESS.md` §1.1 має пункт «self-hosted runner
   на публічному репо — ніколи», якого немає в `docs/19`. Додається разом із рішенням по
   `OQ-30`: після скасування D.2 це єдиний запис, який пояснює, **чому** раннера немає.
   `docs/19` правити можна, `docs/03_DECISIONS.md` — ні.

## 8. Фінал кожної стадії

Покажи ПЛАН → дочекайся OK → виконуй → draft PR → STOP.

У PR обов'язково:

- **«Як перевірити очима»** — 5-10 тверджень, які yurii перевіряє командою або в браузері,
  без читання коду;
- **числові критерії виходу стадії** з дослівним виводом, не переказом;
- **«Опитування»** з §7 у частині, що стосується стадії.

Після merge кожної стадії: запис у `docs/13_PROGRESS_LOG.md` і мануальні кроки — у issue #78
(ADR-036 §4, єдине джерело істини). Журнал зараз відстає на PR #101, #103, #104, #105 —
не збільшуй борг.
