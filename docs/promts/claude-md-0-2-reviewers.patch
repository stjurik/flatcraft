--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -34,19 +34,30 @@
    (Cloudflare Tunnel, гілка = окремий URL), посилання на нього — обов'язкове
    поле повідомлення у `#на-перевірку`. Без живого прев'ю «перевірити очима»
    перетворюється на «прочитати опис», а це не приймання.
-2. **Реалізацію перевіряє не автор.** ШІ-код рев'юїть інший агент (ADR-036:
-   `agy`/Gemini або свіжа Claude-сесія); вердикт публікується ДОСЛІВНО.
+2. **Реалізацію перевіряє не автор.** ШІ-код рев'юїть інший агент (ADR-036) —
+   через `agy`, як описано нижче; свіжа Claude-сесія — лише коли `agy`
+   недоступний, і тоді це прямо пишеться в PR. Вердикт публікується ДОСЛІВНО.
    Розбіжність двох моделей цінніша за їхню згоду — обидві позиції лишаються
    видимими yurii. Мультиагентність — там, де потрібна **незалежність**
    (рев'ю, adversarial-кейси, звірка фактів), не заради кількості агентів.
-   _Автономний режим:_ `agy`/Gemini виконує роль **Рев'юера** (ADR-036 §3), і
-   його вихід — джерело гіпотез, яке верифікує Claude або yurii, а не джерело
-   фактів. Роль не безумовна і тримається на числах:
-   `docs/promts/inputs/agy-stats.md` — **7 викликів, з них 3 з ≥1 вигаданим
-   фактом; чотири останні — 0**. 2026-09-15 зареєстровано поріг: **наступні 10
-   викликів, межа 50%**; якщо частка не впаде нижче, Gemini переводиться з
-   Рев'юера в Розвідника (`docs/20` OQ-35). Кожен виклик логується в
-   `agy-stats.md` — незалогований робить поріг недійсним.
+   _Рецензія через `agy` (рішення yurii 2026-09-24):_ дві моделі — основна
+   **Claude Opus 4.6** і друга, коротка, лише контрприклади, **Gemini 3.8
+   Flash (High)**, модель не з родини Claude. Їхній вихід — джерело гіпотез,
+   яке верифікує Claude або yurii, а не джерело фактів; незгода рецензентів
+   показується в PR обома позиціями дослівно. Роль не безумовна: **кожна
+   модель складає свій іспит — вердикт лише за підсумками її перших 10
+   викликів**; якщо частка викликів з ≥1 вигаданим фактом не нижче 50%,
+   модель іде з Рев'юера в Розвідника. Поріг 10 викликів / 50% зареєстровано
+   2026-09-15 для Gemini (`docs/20` OQ-35), 2026-09-24 yurii поширив його на
+   кожну модель окремо. Так 2026-09-23 з ролі Рев'юера пішов **Gemini 3.1
+   Pro** (10 викликів, 5 з вигадками). Журнал `docs/promts/inputs/agy-stats.md`
+   рахує для кожної моделі і вигадки, і **справжні знахідки** — твердження,
+   що пройшли перевірку й привели до правки; числа рахує
+   `tools/scripts/agy-stats-summary.sh`, тут їх не пишемо — записане число
+   застаріває. Кожен виклик логується в `agy-stats.md` — незалогований робить
+   поріг недійсним. Власні дозволи `agy`: читання лише репо й worktree-ів,
+   запис лише в `docs/promts/inputs/`, жодної команди (`tools/agy/`; доказ —
+   `tools/scripts/agy-permissions-probe.sh`).
 3. **Verify-then-write.** Номери PR, статуси, вміст файлів, дати, API
    бібліотек — з тулів, не з пам'яті. **Вигадувати заборонено**: здогад,
    поданий як факт, — найдорожча помилка в цьому проєкті. Не знаєш — з'ясуй
