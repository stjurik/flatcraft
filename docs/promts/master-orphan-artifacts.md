[MASTER RUN 13 — фіксація артефактів, що існують лише на диску]

МЕТА
Після WIP-коміта z_bracket (`0dcc747`) у робочому дереві лишились чотири
артефакти поза git. Найнебезпечніший — `CLAUDE.md` з новою **§0 «Контракт
виконавця»**: воркфлоу `ai-triage.yml` і `ai-fix.yml` роблять
`actions/checkout` і читають `CLAUDE.md` **з репозиторію**, тобто версію
БЕЗ §0. Контракт, написаний саме для автоматичних агентів, для них невидимий.
Той самий клас проблеми, що й z_bracket, і це вже третій випадок — тому run
додає ще й процесний запобіжник.

Це **docs-only run**. Працюй за CLAUDE.md §0 (читай локальну версію, вона
свіжіша за репозиторну). Роль — Будівельник (docs/15 §0), модель Sonnet.

ЯВНА ЗГОДА YURII (звужена і незвична — прочитай уважно)
`CLAUDE.md` і `.claude/settings.json` — заборонені шляхи (CLAUDE.md §6,
deny-правила docs/16 §1). Згода дана **ЛИШЕ на коміт їхнього поточного
вмісту БЕЗ ЄДИНОЇ ЗМІНИ БАЙТА**. Редагувати їх заборонено — ти переносиш
у git те, що yurii уже написав, і нічого більше. Виняток — Опитування 2
(prettier), і лише за явним вибором yurii.

РЕЖИМ АВТОНОМІЇ (патерн docs/16)
Головне дерево `~/hart` — **read-only**: жодного `git add`, `commit`,
`checkout`, `stash`, `restore`, жодного запису у файли. Воно зараз на гілці
`feat/migrate-z-bracket` з WIP-комітом; цю гілку не чіпати взагалі.
Працюй в ізольованому worktree від свіжого `origin/main`, гілка
`docs/orphan-artifacts`. Один draft PR, НЕ мержити. Force-push заборонено.

КОНТЕКСТ
CLAUDE.md §0, §6, §12, §13 (локальна версія); `docs/02_ROADMAP.md` (розділ
«Ритми»); `docs/16_AUTONOMOUS_RUNS.md` §1 (роль `.claude/settings.json`);
`docs/18_NEW_PART_SPEC.md` і `docs/parts/` (куди належить специфікація
ніжки); issue #78.

──────────────────────────────────────────────
КРОК 0 — ІНВЕНТАРИЗАЦІЯ (читання; нічого не змінює)

A. `git -C ~/hart status --porcelain` — повний вивід у звіт. Очікується
рівно чотири позиції поза git:

- `CLAUDE.md` (modified)
- `.claude/settings.json` (modified)
- `docs/parts/furniture_leg.md` (untracked)
- `docs/promts/master-ai-fix-acceptance.md` (untracked)

**Будь-яка п'ята позиція → STOP і звіт.** Не вгадуй, що це.

B. `sha256sum` кожного з чотирьох файлів у головному дереві — числа
у звіт. Це база для перевірки байт-ідентичності (див. ТЕСТИ).

C. `git -C ~/hart check-ignore -v .claude/settings.json` — чи файл під
`.gitignore`. Якщо так — це свідоме рішення когось у минулому; доповідай
і виноси в Опитування 1, `-f` без згоди не застосовуй.

D. Прочитай `.claude/settings.json` і випиши його вміст у звіт. Якщо там
є абсолютні шляхи конкретної машини, токени, e-mail чи будь-що, чого не
має бути в публічному репозиторії — **STOP**, файл не комітиться,
решта три — комітяться.

E. `npx prettier --check` на `CLAUDE.md`, `docs/parts/furniture_leg.md`,
`docs/promts/master-ai-fix-acceptance.md`. Результат у звіт: якщо хоч
один не проходить — це Опитування 2, бо `--write` змінить байти, а
pre-commit hook інакше не пропустить коміт.

──────────────────────────────────────────────
ДЕЛІВЕРАБЛИ

A. **Перенесення чотирьох файлів у git** (копіювання, не редагування):
скопіюй їх з головного дерева у worktree `docs/orphan-artifacts`,
збережи структуру шляхів, закоміть Conventional Commit'ом
`chore(repo): зафіксувати артефакти, що існували лише на диску`.
У тілі коміта — перелік і причина по кожному:

- `CLAUDE.md` — §0 «Контракт виконавця» + ротація §13 (Master Run 10+11);
  без нього агенти в GitHub Actions читають контракт без §0;
- `.claude/settings.json` — deny-правила, що механічно захищають
  інваріанти §6 (docs/16 §1);
- `docs/parts/furniture_leg.md` — заповнена специфікація Деталі, вхід
  для `new-part-run` (Етап 3 Run 7); містить рішення yurii 2026-07-27/28
  і golden-фікстуру, звірену в SolidWorks;
- `docs/promts/master-ai-fix-acceptance.md` — промпт, що не потрапив
  у жоден PR всупереч конвенції `docs/promts/`.

B. **`docs/02_ROADMAP.md`, розділ «Ритми»** — один рядок у протокол
неділі: перед digest'ом виконати `git status` і переконатись, що жодна
робота не існує лише на диску; знайдене — або коміт у гілку, або свідоме
рішення у треді. Причина (одним реченням у тексті): три випадки поспіль —
міграція z_bracket, §0 у CLAUDE.md, специфікація ніжки.

C. **Issue #78** — коментар із двома пунктами для yurii:

1. Після merge цього PR у головному дереві лишаться локальні копії тих
   самих файлів, і `git pull` впаде на «local changes would be
   overwritten» (як уже сталося з промптами при pull `08a5a45`). Порядок:
   звірити `sha256sum` локального файла з репозиторним → якщо збігається,
   `git restore CLAUDE.md .claude/settings.json` і видалити два untracked
   → потім `git pull`.
2. Позначити виконаним пункт про z_bracket (WIP-коміт `0dcc747`
   у `feat/migrate-z-bracket`, запушено).

D. Цей файл (`docs/promts/master-orphan-artifacts.md`) — включити у PR.

ЩО ЦЕЙ RUN НЕ РОБИТЬ
Не редагує жодного з чотирьох файлів (виняток — Опитування 2). Не пише
в головне дерево. Не чіпає `feat/migrate-z-bracket`. Не мержить. Не
починає Run 7 Етап 2.

ОПИТУВАННЯ (у PR description; дефолт застосовується при merge)

1. **`.claude/settings.json` у git** — заповнити за результатом КРОКУ 0-C/D.
   - ★ Комітити (дефолт, якщо файл не в `.gitignore` і не містить
     машинно-специфічного): deny-правила — проєктний механізм захисту
     інваріантів, а не локальне налаштування; у git вони діють для будь-якої
     сесії й будь-якого клону.
   - Лишити локальним: обирати, якщо КРОК 0 показав `.gitignore` або
     машинно-специфічний вміст.
2. **Якщо `prettier --check` падає на котромусь із файлів.**
   - ★ Застосувати `prettier --write` (дефолт): байти зміняться, але лише
     форматування; зміст лишається байт-у-байт за текстом. Інакше
     pre-commit hook не пропустить коміт. Дифи форматування — у звіт.
   - Закомітити з `--no-verify`: обирати не варто, лишаю для повноти.

ТЕСТИ

- **Байт-ідентичність:** `sha256sum` кожного з чотирьох файлів у worktree
  ПІСЛЯ коміта = значення з КРОКУ 0-B. Обидва набори — у звіт таблицею.
  Розбіжність без вибору Опитування 2 → STOP.
- `git -C ~/hart status --porcelain` ДО і ПІСЛЯ run'у — `diff` порожній.
  Обидва виводи у звіт.
- `git diff --name-only origin/main...HEAD` — рівно: чотири артефакти
  (або три, якщо Опитування 1 = «лишити локальним») + `docs/02_ROADMAP.md`
  - `docs/promts/master-orphan-artifacts.md`. Зайвий файл → STOP.
- `npx prettier --check` на змінених md — зелено.
- **Функціональна перевірка мети:** після коміта в worktree виконай
  `grep -c "Контракт виконавця" CLAUDE.md` — має бути ≥1. Це доводить,
  що §0 справді потрапив у git, а не лишився локальним.

ЯК ПЕРЕВІРИТИ ОЧИМА (розділ у PR, для yurii)

1. У вкладці Files changed PR'у видно `CLAUDE.md`, і в дифі є заголовок
   «## 0. Контракт виконавця».
2. Там же видно новий запис §13 про Master Run 10+11.
3. `docs/parts/furniture_leg.md` присутній, у ньому §8 з числом 198.65.
4. `docs/promts/master-ai-fix-acceptance.md` присутній.
5. У `docs/02` розділі «Ритми» з'явився рядок про `git status` перед digest.
6. У звіті таблиця sha256 «до / після» — усі пари збігаються.
7. У #78 є пункт про порядок дій після merge (sha256 → restore → pull).

ФІНАЛ
Звіт: лінк PR, вивід КРОКУ 0 (A-E), таблиця sha256, консолідоване
«Опитування», підтвердження незайманого головного дерева. STOP —
далі merge yurii, потім Run 7 Етап 2.
