[MASTER RUN 2 — housekeeping + Registry-gate + Feedback-prep: 4 автономні work-packages]

РЕЖИМ АВТОНОМІЇ (пріоритет над усім; патерн docs/16)

- Працюй БЕЗ пауз. Gate «план → OK» = PR-review: план — у PR description
  (PLAN.md НЕ комітити), фінал кожного пакета — DRAFT PR. НІКОЛИ не мержиш.
- НІЧОГО не вигадуй. Бракує факту — перевір у репо. Є ≥2 обґрунтовані варіанти —
  обери задокументований дефолт (CLAUDE.md/ADR/docs/12-15) І додай пункт у
  «ЛИСТ ОПИТУВАННЯ» (див. нижче). Hard-STOP лише: стан репо розходиться з
  описаним; червоні перевірки з фіксом поза скоупом; потрібна міграція drizzle
  (пакет зупиняється на підготовленому schema.ts); файл поза скоупом пакета.
- ЛИСТ ОПИТУВАННЯ: у PR description кожного пакета — розділ «Опитування для
  yurii»: кожен пункт = питання + варіанти + ★рекомендація + «що діє за
  замовчуванням, якщо просто змержити». Наприкінці run'у — консолідований
  список з усіх PR.
- Кожен PR description — розділ «Як перевірити очима» (перевірка готового
  продукту для нетехнічного рев'юера; для docs-PR — які твердження перевірити).
- Головний вектор кожного рішення: масштабованість (новий шаблон/процес без
  правок спільних шарів), ефективність (нуль нових важких залежностей,
  RAM-бюджет MS21), самовдосконалення (рішення обґрунтовуються даними
  телеметрії, де вони вже є).
- Гілки незалежні, кожна від origin/main. Пакети ПОСЛІДОВНО: 0 → 1 → 2 → 3.
- Перед кожним PR: lint + typecheck + tests задіяних workspace зелені
  (виняток WP3 — див. його примітку про міграцію).
- Conventional Commits. Force-push заборонено.

ОБОВ'ЯЗКОВИЙ КОНТЕКСТ
CLAUDE.md, AGENTS.md, docs/14_ARCHITECTURE_EVOLUTION.md (§2, §4.3, §6),
docs/15_LLM_PROMPTS.md (A2, B3, C1, C4 — це ТЗ пакетів), ADR-013/017/019/024/
026/027/031/032, docs/11_OBSERVABILITY.md, docs/02_ROADMAP.md, docs/04_RISKS.md
(R-01, R-10).

ОЧІКУВАНИЙ СТАН (розходження = STOP): Phase 3.3 + 3.3-B у main і активовані;
docs/12 відсутній; docs/promts/inputs/ порожній; ADR-033/034 не існують.

──────────────────────────────────────────────
WP0 — housekeeping (гілка docs/roadmap-sync)

A. docs/02_ROADMAP.md: Phase 3.3 PR 1-6 → [x] з датою 2026-07-06 і PR-номерами
(#54,#55,#56,#60,#58,#59); 5.1 Sentry і 5.2 Umami → [x] з приміткою
«активовано на staging 2026-07-12 (PR #63, #65)». НЕ чіпати інші чекбокси.
B. docs/16_AUTONOMOUS_RUNS.md — дописати 3 патерни останнього циклу:
(1) план у PR description, PLAN.md не комітиться (якщо створювався —
видаляється фінальним комітом); (2) обов'язкові розділи PR: «Питання до
yurii» / «Опитування» / «Як перевірити очима»; (3) перевірка vault-ключів
без значень (diff назв проти .example — команда з Phase 3.3-B).
C. docs/14 §1.4-таблиця: рядки Sentry/аналітика/історія експортів → актуальний
стан (✅ з датою). Нічого більше у 14 не міняти.
D. Включити у цей PR untracked docs/promts/master-registry-run.md (цей файл;
конвенція: промпти живуть у docs/promts/).

──────────────────────────────────────────────
WP1 — C1: інвентаризація шаблонів (гілка docs/inputs-c1)

Виконай C1 з docs/15 САМ (без окремої Haiku-сесії): прочитай усі 6 шаблонів у
packages/types/src/templates/, їх \*-studio/-editor/-viewport у apps/web,
сцени/builders у packages/ui/src/3d-viewport/, Python-модулі workers/cad.
Результат → docs/promts/inputs/c1-template-inventory.md: таблиця аспектів ×
6 шаблонів (✅ однаково / ⚠️ стилістичний дрейф / ❌ поведінковий дрейф) +
топ-10 місць копі-пасту + ОКРЕМО: список знайдених ФАКТИЧНИХ розбіжностей
поведінки (кандидати в баги) — кожна з файл:рядок. Нічого не вигадуй — тільки
те, що є в коді. Це вхід для WP2.

──────────────────────────────────────────────
WP2 — A2: ADR-033 Template Registry, docs-only gate (гілка docs/phase-3-5-registry)

Виконай A2 з docs/15 ПОВНІСТЮ (ADR-033 + docs/12_TEMPLATE_CONTRACT.md +
Roadmap Phase 3.5 checklist), з поправками:

- Вхід — WP1-інвентаризація (цитуй її факти в обґрунтуваннях).
- 6 рішень A2 — кожне ALT/CHOICE/RATIONALE/CONSEQUENCES; спірні (мінімум:
  де живе реєстр — новий packages/templates vs розширення types; доля
  discriminatedUnion ExportRequest; generic editor vs збереження ручних) —
  ДОДАТКОВО в «Опитування» з ★рекомендацією.
- docs/12 містить «Definition of Done нового шаблону» — чеклист, який стане
  шаблоном промпту для кожного майбутнього виробу (мета вектора: 1 TS-модуль +
  1 Python-модуль + снапшоти, нуль правок web/api).
- Roadmap Phase 3.5: PR-план — registry-пакет + conformance-suite перший,
  далі міграція шаблонів ПО ОДНОМУ від найпростішого (perforated_panel) до
  найскладнішого (enclosed_shelf), видалення per-template файлів, e2e зелені
  після КОЖНОГО PR.
- Інваріанти, які контракт НЕ сміє ламати (перерахуй в ADR явно): байт-у-байт
  DXF/PDF, render-gate ADR-026, products ADR-027, browser-safe entry
  cad-engine, серверна валідація ADR-019.

──────────────────────────────────────────────
WP3 — Phase 3.4: QR-фідбек з виробництва (гілка feat/phase-3-4-qr-feedback)

Виконай B3 з docs/15 ПОВНІСТЮ, з поправками:

- Спершу C4 (UX-тексти форми UA+EN, 2 варіанти) — виконай сам, поклади в
  docs/promts/inputs/c4-feedback-copy.md, обери варіант за критерієм «коротше і
  тепліше», другий залиш в «Опитуванні».
- QR у PDF: СПОЧАТКУ перевір, куди він веде зараз (workers/cad/export/pdf.py);
  зміна URL на {BASE_URL}/f/{export_id} → снапшоти перегенеруй ОКРЕМИМ комітом
  з поясненням (свідома зміна, детермінізм зберігається).
- packages/db: export_feedback у schema.ts — і hard-STOP пакета на міграції:
  PR лишається draft з розділом «Мануальний крок yurii: pnpm --filter
  @flatcraft/db generate → migrate → commit → зняти draft» (як #55).
  Позначи у PR, що int-тести червоні ДО міграції — очікувано.
- Форма /f/[exportId]: мобільна-перша (360px), без auth, 3 поля, rate-limit
  IP, 404 на невідомий export_id; телеметрія: подія feedback_submitted у
  events (типи вже в packages/types/events — розшир за патерном).
- Digest: додай секцію deviation-репортів у buildDigest (плейсхолдер з ADR-032
  вже є) — з юніт-тестом на фікстурі.
- docs: 06_API_CONTRACT (POST /feedback/{export_id}), 04_RISKS R-01 mitigation
  4 → закрито з датою, 11_OBSERVABILITY — нові події.

──────────────────────────────────────────────
ФІНАЛ RUN'У
Підсумкове повідомлення:

1. Таблиця 4 PR (гілка, статус checks, що чекає yurii).
2. КОНСОЛІДОВАНИЙ ЛИСТ ОПИТУВАННЯ — всі пункти з усіх PR, кожен: питання /
   варіанти / ★рекомендація / дефолт-при-merge. Згрупуй: «блокує merge» vs
   «можна вирішити пізніше».
3. Мануальний чеклист yurii по порядку: merge WP0 → merge WP1 → рев'ю ADR-033
   за опитуванням → merge WP2 → міграція для WP3 → зняти draft → merge WP3 →
   нагадування: §7.6-7.7 website_id (якщо ще не зроблено) і недільний
   acceptance digest.
   STOP.
