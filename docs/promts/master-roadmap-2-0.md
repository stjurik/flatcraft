[MASTER RUN 6 — Roadmap 2.0: треки + ритми + мультиагентний конвеєр (docs-gate)]

МЕТА
Перевести модель розробки з «фаз і спринтів» (виконали свою роль — MVP є)
на «треки + ритми + конвеєр агентів»: Roadmap стає картою черг, двигун
розвитку — щотижневий digest і щомісячний huddle, виконавці — агенти з
чіткими ролями, yurii — власник гейтів і оцінювач готового продукту.
Це docs-only gate: код не змінюється.

РЕЖИМ АВТОНОМІЇ (патерн docs/16)

- Гілка docs/roadmap-2-0 від свіжого origin/main (merge-base guard).
- Без пауз; план — у PR description; фінал — ОДИН draft PR. НЕ мержити.
- Verify-then-write: статуси/номери PR/дати — лише з gh/git-фактів.
- ≥2 варіанти → «Опитування» (питання / варіанти / ★ / дефолт-при-merge).
- «Як перевірити очима»: 8-10 тверджень нового Roadmap для вибіркової
  перевірки yurii (кожне має бути правдою).
- ПЕРЕДУМОВА-GATE: у CLAUDE.md §13 є «Feature 3.4» (ротацію 2026-07-17
  закомічено); відкритих PR попередніх run'ів нема. Розходження → STOP.

КОНТЕКСТ (прочитати до старту)
CLAUDE.md (§6, §11 — застарілий sprint-loop, §12, §13), AGENTS.md,
docs/02 (поточна структура — після housekeeping), docs/03 (ADR-020, 032,
033 + індекс), docs/12, docs/13, docs/14 (§6 — стара фазова послідовність),
docs/15 (§0 маршрутизація), docs/16, docs/17 якщо існує,
docs/promts/master-ai-bugfix-flow.md (готовий, ще не запущений).

ЗАФІКСОВАНІ РІШЕННЯ YURII (не переглядати)

- Модель: треки + ритми (замість фаз/дат) — підтверджено 2026-07-17.
- Мультиагентні ролі: Архітектор (сильна модель, master-runs/ADR/тріаж),
  Будівельник (Sonnet, імплементація за планом), Рев'юер (незалежне рев'ю
  AI-PR), Розвідник (дешеві C-задачі).
- Gemini CLI: роль Reviewer/Scout — УМОВНА, статус «pending: yurii ще не
  перевірив доступ CLI за своєю підпискою». В ADR фіксується як варіант
  з тригером активації, БЕЗ інтеграції зараз.
- «Черга yurii»: один закріплений issue — єдине місце ВСІХ мануальних
  кроків; кожен run зобов'язаний оновлювати його замість розкидання
  чеклистів по PR (урок: загублена міграція #69, тиждень простою).
- Рішення repo-visibility (private/ліцензія) і A8-runner — НЕ приймаються
  цим run'ом: заводяться у трек T5 як відкриті рішення з ★рекомендаціями.

ДЕЛІВЕРАБЛИ
A. ADR-036 «Модель розробки 2.0: треки, ритми, мультиагентний конвеєр»
у docs/03 (+рядок в індекс). Рішення (кожне ALT/CHOICE/RATIONALE/
CONSEQUENCES):

1.  Roadmap як треки-черги без дат vs фази vs kanban-tool поза репо.
2.  Ритми: нд digest → кожен пункт = issue АБО «accepted noise»
    (запис у digest-треді); 1-е число місяця — huddle A4 (вхід: 4 digest
    - Roadmap; вихід: корекція черг треків).
3.  Ролі агентів + маршрутизація моделей (оновлює docs/15 §0);
    Reviewer: сильна Claude-модель ЗАРАЗ, Gemini — після перевірки
    доступу (тригер активації описати).
4.  «Черга yurii» — закріплений issue; протокол: run додає пункти
    секцією з датою, yurii викреслює виконане; PR-чеклисти лишаються
    ДУБЛЕМ, не джерелом істини.
5.  Гейти незмінні: merge — yurii (винятки — лише разовою явною згодою
    у конкретному run'і); CLAUDE.md/міграції/infra — за наявними
    правилами §6.
    B. docs/02_ROADMAP.md — повний перепис:
6.  Шапка: призначення файлу, посилання на docs/13 (історія) і
    docs/14 (стратегія).
7.  П'ять треків, кожен: мета → черга (нумерований список «наступний
    PR/run» з готовими посиланнями на промпти docs/promts/\*) → гейт yurii:
    - T1 Стабілізація: запуск master-ai-bugfix-flow (перший у черзі!),
      потік «digest/Sentry → issue → ai-fix», TD-01/TD-02 з беклогу.
    - T2 Registry (ADR-033): PR2 registry-пакет+conformance → міграція
      6 шаблонів по одному (порядок: perforated_panel → l_bracket →
      z_bracket → corner_angle → wall_shelf → enclosed_shelf) →
      видалення per-template файлів. Старт — після першого тижня
      телеметрії (дані можуть змінити порядок шаблонів).
    - T3 Launch: digest-acceptance 2026-07-19 → go-live PR за docs/08 §8
      → KPI-тиждень → юрист для /privacy+/terms.
    - T4 Еволюція платформи: A3 (ADR-034 process layer, docs-gate) →
      FDM-друк v1.2 (обидва — ПІСЛЯ T2).
    - T5 Інфраструктура: рішення repo-visibility (варіанти + ★private,
      наслідки з аналізу 2026-07-14) → A8: staging/CI-runner + uptime-
      експеримент (квартал даних → рішення про міні-ПК/prod-переїзд).
8.  KPI (перенести з поточного 02 без змін) + Tech-debt беклог +
    «Не робимо» (анти-цілі з docs/14 §5).
9.  Архів: завершені фази → компактна таблиця (фаза / дата / PR /
    посилання на docs/13) ЗАМІСТЬ повних секцій. Історія не губиться —
    вона в docs/13; чекбокс-простирадла з 02 прибрати.
10. Ритми (розділ): протокол неділі (15 хв) і huddle (промпт A4
    з docs/15).
    C. docs/15 §0: оновити таблицю маршрутизації під ролі з ADR-036
    (+рядок Reviewer із приміткою про Gemini-умову).
    D. docs/14 §6: таблицю порядку впровадження позначити «виконано /
    замінено треками — див. docs/02» (не переписувати історію, лише статус).
    E. Закріплений issue «Черга yurii» (gh issue create + gh issue pin):
    початкове наповнення ЛИШЕ фактичними відкритими пунктами (звір з
    репо/розмов): digest-acceptance 19.07; тест Gemini CLI доступу;
    рішення T5 (repo visibility + runner); юрист (T3); stash pop на
    chore/guard-branch-base. Формат: чекбокси з датою додавання і
    посиланням на джерело.
    F. У PR description — розділ «РУЧНА ПРАВКА CLAUDE.md ДЛЯ АРХІТЕКТОРА»:
    готовий текст заміни §11 «Цикл розробки (sprint loop)» на цикл 2.0
    (трек → черга → run/PR → гейт yurii → ритми; ≤ 15 рядків, стиль §11).
    G. Цей файл (docs/promts/master-roadmap-2-0.md) — включити у PR.

ТЕСТИ
prettier/lefthook md зелені; посилання у новому 02 валідні (кожен
згаданий файл docs/promts/\* існує); жодних правок поза docs/ і issue.

ФІНАЛ
Звіт: PR-лінк, «Опитування» (консолідоване), лінк на закріплений issue
«Черга yurii», нагадування архітектору про §11-текст. STOP.
