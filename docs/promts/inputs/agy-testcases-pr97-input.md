# Завдання: додаткові тест-кейси до PR #97 (роль Тест-інженер, ADR-035 §4)

Ти — незалежний Тест-інженер проєкту flatcraft. Твоя мета — знайти те, чого
НЕ покриває наявний регресійний тест. Claude потім верифікує кожен твій кейс
проти реального коду; свідомо невірний або тавтологічний кейс буде відхилений
із зазначенням причини, тож пропонуй лише те, у чому впевнений.

**Інструменти:** ЛИШЕ `read_file` і `write_file`. НЕ використовуй жодних
command/bash/terminal інструментів.

## Контекст

PR #97 прибирає пункт «Telegram» із футера (компонент `SiteLinks`), разом із
ключем словника `siteLinks.telegram` в інтерфейсі та обох локалях (uk/en).

Прочитай:

1. `/home/yurii/hart/docs/promts/inputs/_review-pr-97.diff` — diff PR.
2. `/home/yurii/hart/apps/web/src/components/site-links.tsx` — компонент
   (стан ДО фіксу; diff показує зміну).
3. `/home/yurii/hart/apps/web/src/i18n/dictionaries.ts` — словники uk/en і
   інтерфейс `Dictionary` (структура ключів `common.siteLinks`).
4. `/home/yurii/hart/apps/web/tests/e2e/about.spec.ts` — наявні e2e-тести
   футера, серед них «Footer SiteLinks «Про проєкт» веде на /about (не /soon)»
   і «Footer Discord-лінк — зовнішній інвайт (не /soon)». Новий тест PR'а
   («без пункту Telegram») є у diff'і.

## Наявне покриття (НЕ дублюй його)

Новий тест PR'а вже перевіряє: усередині `getByTestId("site-links")` кількість
лінків з іменем "Telegram" дорівнює 0 — на `/` і на `/en`.

## Що запропонувати

5-10 ДОДАТКОВИХ кейсів для зачепленої зони. Кожен — з очікуванням у форматі
`дія/вхід → очікуваний результат`. Пріоритет — те, що діагональ diff'а могла
пропустити:

- позитивні перевірки сусідніх пунктів колонки «Спільнота» (щоб видалення не
  зачепило GitHub/Discord: наявність, href, `target="_blank"`);
- цілісність решти колонок футера (Продукт/Юридичне) в обох локалях;
- паритет структури словників uk/en (кількість і набір ключів
  `common.siteLinks` мають збігатися) — це вже unit-рівень, не e2e;
- сторінка `/soon` лишається досяжною через інший пункт («Розблокувати» /
  "Unlock"), тобто маршрут не осиротів;
- негативні/межові випадки: чи не лишилось «Telegram» деінде на сторінці поза
  футером; чи немає осиротілого ключа перекладу.

Для кожного кейсу вкажи рівень: `e2e (Playwright)` або `unit (Vitest)`, і
файл, куди його логічно покласти.

## Формат виходу

Запиши РІВНО у файл
`/home/yurii/hart/docs/promts/inputs/testcases-issue-84.md`.
Жодних інших файлів не чіпай.

Нумерований список; кожен пункт: назва кейсу — рівень — файл — вхід/дія →
очікування — навіщо (яку саме регресію ловить).
