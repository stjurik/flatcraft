1. Наявність та коректність лінка GitHub у футері — e2e (Playwright) — `/apps/web/tests/e2e/about.spec.ts` — Перехід на `/` (або `/en`) → лінк "GitHub ↗" всередині `site-links` існує, `href` дорівнює `https://github.com/stjurik/flatcraft`, `target` дорівнює `_blank` — Перевіряє, що видалення Telegram не зачепило сусідні пункти в колонці «Спільнота» і лінк GitHub працює коректно.
2. Наявність та коректність лінка Discord у футері для EN локалі — e2e (Playwright) — `/apps/web/tests/e2e/about.spec.ts` — Перехід на `/en` → лінк "Discord ↗" всередині `site-links` існує, має зовнішній `href` та `target="_blank"` — Перевіряє, що сусідній пункт Discord залишився цілим в англійській локалі.
3. Цілісність колонок футера в обох локалях — e2e (Playwright) — `/apps/web/tests/e2e/about.spec.ts` — Перехід на `/` та `/en` → перевірити наявність заголовків колонок ("Продукт", "Спільнота", "Юридичне" для uk; "Product", "Community", "Legal" для en) всередині `site-links` — Гарантує, що видалення одного пункту не зламало рендер цілих колонок.
4. Паритет ключів common.siteLinks між uk та en — unit (Vitest) — `/apps/web/tests/unit/dictionaries.test.ts` — Отримати `Object.keys(dictionaries.uk.common.siteLinks)` і `Object.keys(dictionaries.en.common.siteLinks)` → набір ключів має збігатися на 100% (і їх кількість дорівнює 12) — Гарантує строгий паритет словників на рівні ключів, підтверджуючи, що жодна локаль не пропущена при видаленні.
5. Відсутність ключа telegram у словниках — unit (Vitest) — `/apps/web/tests/unit/dictionaries.test.ts` — Перевірити `dictionaries.uk.common.siteLinks` та `dictionaries.en.common.siteLinks` → ключа `telegram` не повинно існувати — Негативний випадок: ловить осиротілий ключ перекладу, який міг би залишитись у рантаймі.
6. Доступність сторінки /soon через інший пункт футера — e2e (Playwright) — `/apps/web/tests/e2e/about.spec.ts` — Перехід на `/` (та `/en`) → клік по пункту "Розблокувати" (або "Unlock") → відкривається сторінка, URL містить `/soon` — Перевіряє, що маршрут `/soon` не осиротів і залишається досяжним через інший лінк у футері.
7. Глобальна відсутність слова Telegram на сторінці — e2e (Playwright) — `/apps/web/tests/e2e/about.spec.ts` — Перехід на `/` та `/en` → `page.getByText('Telegram')` → кількість збігів дорівнює 0 — Негативний/межовий випадок: переконується, що слово не залишилось деінде на сторінці поза компонентом `site-links`.
