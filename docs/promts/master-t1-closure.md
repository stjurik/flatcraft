[MASTER RUN 12 — закриття треку T1: документальний борг + правдива черга]

МЕТА
Привести T1 «Стабілізація» у стан, де черга показує реальність, а не 19 липня.
Три з чотирьох пунктів T1 описані невірно: №1 «стоїть на незмерженому PR #86»
(насправді acceptance пройдено наскрізь), №3 TD-01 фактично виконано різними
PR і ніхто не викреслив, №4 TD-02 не зроблено і лишається наступним. Плюс
документальний борг Master Run 10 крок 12: ADR-035 досі подає спростовану
гіпотезу як рішення.

Це **docs-only run**. Жодних змін у `apps/`, `packages/`, `workers/`,
`infra/`, `.github/`. Працюй за CLAUDE.md §0. Роль — Будівельник
(docs/15 §0), модель Sonnet.

ЯВНА ЗГОДА YURII (звужена)
`docs/03_DECISIONS.md` — заборонений шлях (CLAUDE.md §6). Згода дана ЛИШЕ на
**додавання примітки про фактичний результат** до ADR-035. Саме рішення
ADR-035 НЕ переглядається: двигун лишається Claude, причина п'яти падінь —
недійсне ЗНАЧЕННЯ секрету, а не непридатність Pro-токенів у CI. Якщо в процесі
з'ясується, що потрібна зміна рішення, а не запис результату → STOP зі звітом.

РЕЖИМ АВТОНОМІЇ (патерн docs/16)
Гілка `docs/t1-closure` від свіжого `origin/main`, merge-base guard.
Один draft PR, НЕ мержити. Force-push заборонено. План — першим розділом
PR description.

**ОКРЕМЕ ЖОРСТКЕ ПРАВИЛО ЦЬОГО RUN'У.** У головному робочому дереві
`~/hart` лежить незакомічена міграція z_bracket. Ти НЕ маєш права її
торкатись: жодного `git add`, `git commit`, `git stash`, `git checkout`,
`git restore` у головному дереві. Працюй в ізольованому worktree. Єдине
дозволене щодо неї — читання (`git status --porcelain`, `git diff --stat`).
Порушення = STOP і повний звіт.

КОНТЕКСТ (прочитати до старту)
CLAUDE.md §0, §6, §11, §13; `docs/02_ROADMAP.md` (трек T1 цілком);
`docs/03_DECISIONS.md` ADR-035 і ADR-036; `docs/13_PROGRESS_LOG.md` (формат
записів — наслідуй, не винаходь); `docs/17_AI_BUGFIX_FLOW.md` (§8, §9,
§«Конфлікт з Run 7»); `docs/promts/ai-review-local.md`;
`docs/promts/inputs/agy-stats.md`; issue #78 (закріплена «Черга yurii»).

──────────────────────────────────────────────
КРОК 0 — ІНВЕНТАРИЗАЦІЯ (verify-then-write; нічого не змінює)

Усі факти нижче звір ФАКТИЧНО через `gh`/`git`. Числа й SHA з цього промпту —
орієнтир для пошуку, НЕ джерело істини; розбіжність фіксуй у звіті.

A. **Хронологія конвеєра.** `gh pr view` для #86, #94, #95, #97: номер, SHA
merge-коміта, дата. Прогони: перший зелений тріаж (шукати серед
`gh run list --workflow=ai-triage.yml`), деплой після #97. Стан #84
(ким і чим закритий). Результат — таблиця у PR description.

B. **z_bracket у робочому дереві — ЛИШЕ опис.** `git -C ~/hart status
--porcelain` і `git -C ~/hart diff --stat` (читання, не зміна). Дай у
звіті: скільки файлів, які пакети зачеплені, чи є серед них заборонені
шляхи, чи дерево містить ще щось, крім z_bracket. Свою оцінку «доробити /
зафіксувати WIP-комітом / відкинути» ОЗВУЧ, але НЕ виконуй — це гейт yurii
(Опитування 2).

C. **TD-01 — звірка з реальністю.** Пройди по всіх `.github/workflows/*.yml`
і випиши фактичні версії дій (`actions/*`, `pnpm/action-setup`,
`docker/*`) + значення `NODE_VERSION` у `ci.yml`. Таблиця «у TD-01
написано | фактично» у PR description.

D. **TD-02 — звірка з реальністю.** Чи є зараз `*.js` у `packages/*/src/`
і `apps/*/src/` (git + локально); чи є в `lefthook.yml` правило, що
падає на їхню появу. Один абзац висновку.

──────────────────────────────────────────────
ДЕЛІВЕРАБЛИ

A. **`docs/03_DECISIONS.md` — ADR-035, примітка «Фактичний результат
(2026-08-03)»** (текст рішення не чіпати):

- acceptance пройдено наскрізь: issue → тріаж у CI → `ai-approved` →
  Будівельник у CI → незалежне рев'ю agy → merge → staging;
- гіпотеза, що фіксом була зміна моделі тріажу, **спростована**; п'ять
  падінь спричинило недійсне значення `CLAUDE_CODE_OAUTH_TOKEN`;
- дорогою перевірено й відкинуто чотири гіпотези (opus-entitlement, квота,
  версія дії, непридатність Pro-токена в CI) — перелічити, бо саме вони
  коштували тижня;
- рішення 3 (двигун Claude, рев'ю локальне мультиагентне) лишається чинним
  без ревізії.

B. **`docs/17_AI_BUGFIX_FLOW.md`**

1. §8 — крок «створити лейбли» позначити виконаним (дата), і додати
   правило: мануальні кроки setup-run'у заносяться в #78 У МОМЕНТ
   створення PR (CLAUDE.md §0 п.5).
2. §«Конфлікт з Run 7» — переписати з розпливчастого на механічне:
   **точний перелік шляхів**, які тріаж зобов'язаний класифікувати як
   ARCHITECTURAL, поки триває Registry-міграція (виведи зі структури
   `apps/web` і `packages/templates` фактично, не з пам'яті), і **явний
   тригер зняття**: завершення Етапу 2 Run 7 (міграція шести шаблонів,
   `docs/promts/master-registry-track.md`). Додай речення: «Цей розділ —
   тимчасовий; його видалення — окремий пункт у #78».
3. §9 — рядок про компенсуючий ручний контроль: поки в `ai-fix.yml`
   немає `pnpm install`, Будівельник не може прогнати тести, тож
   «червоний до фікса» перевіряє рев'юер вручну (див. C).

C. **`docs/promts/ai-review-local.md`** — два обов'язкові пункти чеклиста:

- «Відкотити фікс (лишивши тест), переконатись, що тест ПАДАЄ, повернути
  фікс, переконатись, що проходить. Без цього рев'ю не завершене» —
  з поміткою, що пункт зникне, коли з'явиться test-first proof job (A8).
- `npx prettier --write docs/promts/inputs/` одразу після кожного виклику
  `agy` (дослівна версія лишається у коментарі PR).

D. **`docs/promts/inputs/agy-stats.md`** — додати колонку «вигаданих фактів
(скільки / які)». Заповнити ретроспективно два наявні рядки за фактами
з Master Run 8 і рев'ю #97 (вигаданий шлях `apps/web/tests/unit/`,
неперевірене припущення про червоний тест), не вигадуючи нових.

E. **`docs/02_ROADMAP.md`, трек T1** — переписати чергу під реальність:

1. Acceptance конвеєра — **закрито** (дата, PR, посилання на docs/13).
2. Постачання сигналу «дайджест/Sentry → issue» — за Опитуванням 1.
3. TD-01 — **закрито**; НОВИМ окремим рядком черги:
   «`NODE_VERSION` 20.11.0 → 22 або 24» з фактом: Node 20 досяг EOL
   30.04.2026, тобто рантайм збірки без security-патчів; цілі —
   22 (EOL 04.2027) або 24 (EOL 04.2028). **Не приміткою під закритим
   пунктом** — окремим пунктом черги.
4. TD-02 — лишається, з приміткою для майбутнього issue: перевага
   scope'у «скрипт `tools/scripts/*.sh` + тест на рівні shell», бо
   Будівельник у CI не має `node_modules` і vitest прогнати не зможе.

F. **`docs/13_PROGRESS_LOG.md`** — запис нагору «Master Run 10+11 — перший
наскрізний прохід ai-bugfix конвеєра»: факти, PR/SHA, прогони, метрика agy
(вердикт, прийнято/відхилено кейсів, scope-creep), і ОБОВ'ЯЗКОВО — ціна:
п'ять падінь тріажу, чотири спростовані гіпотези, дірка «зелений прогін
без зміни стану issue». Запис має бути корисним тому, хто читатиме його
через рік, а не переможним.

G. **Текст ротації CLAUDE.md §13** — готовим блоком у PR description
(«РУЧНА ПРАВКА ДЛЯ АРХІТЕКТОРА»). Сам файл НЕ чіпати (§6).

H. **Issue #78** — коментар: викреслити виконане (лейбли, reopen #84,
merge #95, `show_full_output`); нова секція **«Відкладено до A8»** з
тригером активації: `pnpm install` у `ai-fix.yml`, test-first proof job,
path-checker вердиктів agy, read-only пісочниця для agy; окремий пункт
«зняти §Конфлікт з Run 7 після Етапу 2 Run 7»; пункт про z_bracket за
результатом КРОКУ 0-B.

I. Цей файл (`docs/promts/master-t1-closure.md`) — включити у PR.

ЩО ЦЕЙ RUN НЕ РОБИТЬ
Не торкається робочого дерева `~/hart` (див. правило вище). Не заводить
issue для TD-02 — це крок yurii після merge. Не ревізує ADR-035. Не міняє
воркфлоу. Не мержить.

ОПИТУВАННЯ (у PR description; дефолт застосовується при merge)

1. **Постачання сигналу в конвеєр.**
   - ★ Лишити ручним ритуалом (дефолт): недільний digest ≤15 хв, кожен
     пункт → issue АБО «accepted noise» у треді; Sentry — очима. Причина:
     10 користувачів/день, обсяг сигналу малий, автоматика постачання
     коштує більше, ніж економить. Тригер перегляду: digest стабільно
     дає >5 пунктів або аудиторія росте кратно.
   - Автоматизувати зараз (Sentry webhook → issue-чернетка). Наслідок:
     нова інтеграція і новий клас шуму до того, як є що фільтрувати.
2. **Доля незакоміченої міграції z_bracket** (виконує yurii, не run).
   - ★ Зафіксувати WIP-комітом у гілці `feat/registry-z-bracket` і
     запушити: робота перестає існувати лише на диску, нічого не
     втрачається, merge не потрібен.
   - Доробити в межах Run 7 Етапу 2 просто зараз.
   - `git stash` з іменем.
   - Відкинути.

ТЕСТИ

- `npx prettier --check` на всіх змінених md — зелено.
- Кожне посилання, додане в `docs/02` і `docs/13`, веде на існуючий файл
  або на реальний PR/issue (перевір `gh`, не на око).
- `git diff --name-only origin/main...HEAD` — рівно: `docs/02_ROADMAP.md`,
  `docs/03_DECISIONS.md`, `docs/13_PROGRESS_LOG.md`,
  `docs/17_AI_BUGFIX_FLOW.md`, `docs/promts/ai-review-local.md`,
  `docs/promts/inputs/agy-stats.md`, `docs/promts/master-t1-closure.md`.
  Будь-який зайвий файл → STOP.
- `git -C ~/hart status --porcelain` ДО і ПІСЛЯ run'у — ідентичний вивід
  (доказ, що робоче дерево не зачеплено). Обидва виводи — у звіті.

ЯК ПЕРЕВІРИТИ ОЧИМА (розділ у PR, для yurii — без читання коду)

1. У `docs/02` трек T1: пункти 1 і 3 позначені закритими, і серед черги
   з'явився новий рядок про Node 22/24.
2. У `docs/02` немає жодної згадки «стоїть на незмерженому PR #86».
3. В ADR-035 з'явився блок «Фактичний результат» зі словом «спростована»
   про гіпотезу зміни моделі; сам текст рішення не змінився.
4. У `docs/17` §«Конфлікт з Run 7» є конкретний список шляхів і речення
   про те, коли цей розділ видаляється.
5. У `docs/promts/ai-review-local.md` є пункт про відкат фікса й червоний
   тест.
6. У `docs/13` перший запис — про Master Run 10+11, і в ньому названо
   п'ять падінь і чотири спростовані гіпотези.
7. У #78 є секція «Відкладено до A8» з чотирма пунктами.
8. У звіті run'у два `git status` головного дерева збігаються рядок
   у рядок.

ФІНАЛ
Звіт: лінк PR, результати КРОКУ 0 (A-D) окремими таблицями, консолідоване
«Опитування», підтвердження переліку змінених файлів і незайманого робочого
дерева. STOP — далі гейти yurii.
