# 17. AI Bugfix Flow v2 — інструкція yurii

> **Кому це:** yurii — єдина людина, що просуває цей конвеєр (лейбли, approve, merge).
>
> **Що це:** як завести дрібний баг/покращення так, щоб він пройшов конвеєр тріаж → фікс → рев'ю без ручної Claude Code-сесії, і що робити на кожному лейблі.
>
> **Архітектурне рішення:** ADR-035 (`docs/03_DECISIONS.md`). Джерело промпту: `docs/promts/master-ai-bugfix-flow.md`.

---

## 1. Життєвий цикл одного issue

```
[issue за формою] → лейбл ai-fix → (ai-triage.yml) → ai-plan-ready
                                                     → ai-architectural (STOP, звичайний трек)
                                                     → ai-need-info (відповісти → знову ai-fix)

ai-plan-ready → (прочитав план) → лейбл ai-approved → (ai-fix.yml) → draft PR
                                                                    → ai-architectural (STOP, PR немає)

draft PR → docs/promts/ai-review-local.md (одна команда, локально) → merge (ти)
```

Кожна стрілка — ручна дія з твого боку, окрім двох `(ai-*.yml)` — це GitHub Actions,
запускаються самі, щойно з'являється відповідний лейбл.

## 2. Як завести issue

Дві форми (`.github/ISSUE_TEMPLATE/`): **🐛 Баг** (`bug_report.yml`) і
**✨ Покращення** (`improvement.yml`). Заповни поля — вони структуровані
навмисно (Де / Кроки / Очікував / Побачив / Скріншот / Пристрій), тріаж це
читає першим.

**Лейбл `ai-fix` НЕ ставиться формою автоматично** (свідомо, ADR-035 §5.1) —
постав його сам, ПІСЛЯ того, як прочитав issue і вирішив, що варто спробувати
автоматичний фікс.

## 3. Тріаж (`ai-triage.yml`) — що читати

Приходить коментар з одним із трьох результатів:

- **`ai-plan-ready`** — root-cause гіпотеза + план (файли у скоупі, який
  регресійний тест додасться, рекомендована модель) +, за потреби, розділ
  «Опитування» (варіанти з ★ рекомендацією). **Прочитай план ПОВНІСТЮ ПЕРЕД**
  тим, як ставити `ai-approved` — це і є твоя точка контролю, аналог
  «прочитай ADR перед B-промптом» з `docs/15` §0 п.3.
  - Погоджуєшся з планом (і, якщо було «Опитування» — обрав варіант,
    зазвичай ★) → постав лейбл `ai-approved`.
  - Не погоджуєшся → коментар з правками, лейбл лишається `ai-plan-ready`
    (жоден workflow на нього не реагує — можеш перезапустити тріаж, знявши
    і знову поставивши `ai-fix`, або просто продовжити вручну).
- **`ai-architectural`** — тріаж вирішив, що це не дрібний фікс: зачіпає
  §7/§13-інваріанти, спільні шари, схему БД, infra, або Registry-зону
  (§6 нижче). Це СТОП для **цього конвеєра**, але НЕ кінець роботи: коментар
  тріажу зобов'язаний містити рішення-запит, а твоя відповідь переводить
  задачу у чергу треку. Повний протокол — **§3-bis нижче**.
- **`ai-need-info`** — 1-3 конкретні питання. Відповідай прямо в issue,
  потім знову постав `ai-fix` (новий прогін тріажу побачить твою відповідь
  через `gh issue view --json comments`).

## 3-bis. Шлях із ARCHITECTURAL — не кінець, а перехід у чергу рішень

`ai-architectural` зупиняє **конвеєр**, а не **роботу**. Стан читається як
«задача чекає на інженерне рішення yurii», і в нього є вихід. До Master Run 15
виходу не було: тріаж ставив лейбл, писав коментар — і на цьому все
закінчувалось.

### Що зобов'язаний написати тріаж

Коментар із класифікацією `ARCHITECTURAL` містить **шість блоків**. Коментар,
у якому бракує хоч одного, вважається неповним.

| Блок                 | Що в ньому                                                                                                                                                |
| -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Класифікація**     | `ARCHITECTURAL` + які саме правила спрацювали (інваріант CLAUDE.md §7/§13, спільний шар, схема БД, `infra/`, Registry-зона §6). Якщо правил кілька — усі. |
| **Скоуп**            | Файли `шлях:рядок`, знайдені пошуком по коду, а не по пам'яті. Окремо позначені ті, що лежать у заборонених для агента шляхах (ADR-035 §5.4).             |
| **Питання**          | ОДНЕ речення по суті задачі: що це означає для користувача або для виробу. Не про код.                                                                    |
| **Варіанти**         | 2-4 штуки, у кожного — **наслідок**: що зміниться у продукті, у файлах, у документах.                                                                     |
| **★ Рекомендація**   | Один варіант — або дослівно «рекомендації немає: питання про фізичний зміст, а не про код». Порожнім лишати не можна.                                     |
| **Оборотність+трек** | `оборотне`/`незворотне` і трек `docs/02_ROADMAP.md`, який підхопить роботу після відповіді.                                                               |

Поля «дефолт-при-мовчанні» тут немає **свідомо**: ARCHITECTURAL — це питання
**класу A** (ADR-039 §6, «незворотне або зміна контракту»), а в класу A дефолту
не буває за визначенням. Агент чекає на відповідь; мовчання нічого не вирішує.

### Що робиш ти

1. **Відповідаєш коментарем прямо в issue** — обраний варіант і, якщо треба,
   чому саме він.
2. **Знімаєш лейбл `ai-architectural` разом із відповіддю.** Лейбл — індикатор
   «чекає на рішення»; поки він висить, задача рахується нерозв'язаною.
3. **Заносиш задачу у чергу названого треку** `docs/02_ROADMAP.md`. Це і є
   момент, коли гілка закривається: відповідь без запису у чергу не рухає
   задачу нікуди — рівно це й сталося з #96 (нижче).

Якщо твоя відповідь **знімає архітектурність** (наприклад, з'ясувалось, що
справжній скоуп менший, ніж вважав тріаж) — постав `ai-fix` заново. Новий
прогін побачить відповідь через `gh issue view --json comments` і може
перекласифікувати задачу у `LOCAL-FIX`.

### Нотифікація

Поки Discord-каналів автономного середовища немає (ADR-039, крок 5 треку T5)
— єдиний канал нотифікації GitHub mobile. Після появи демона рішення-запит
дублюється у `#питання-блокуючі` як питання класу A з тими самими шістьма
блоками.

### Механічна перевірка повноти — чого сьогодні немає

Крок «Verify issue reached a terminal state» (`ai-triage.yml`) перевіряє
**лейбл**, не **зміст коментаря**: коментар без рішення-запиту сьогодні
проходить зеленим. Повнота шести блоків тримається на тексті промпту, а не на
гейті. Це відомий і свідомо прийнятий розрив — реалізація разом із демоном
(ADR-039, крок 5), який і так парсить коментарі; окремий пункт у #78.

### Чому цей розділ існує

Issue **#96** («Прибрати параметр "напрям згину" для деталі L-кронштейн») —
доказ. Хронологія з фактів `gh` (UTC):

| Коли             | Що                                                                    |
| ---------------- | --------------------------------------------------------------------- |
| 2026-08-01 11:51 | yurii завів issue і поставив `ai-fix`                                 |
| 2026-08-01 11:52 | тріаж класифікував `ARCHITECTURAL`, зняв `ai-fix` — 1 хв 50 с         |
| 2026-08-01 12:21 | yurii відповів коментарем по суті                                     |
| 2026-08-12       | лейбл той самий, черга треку порожня, роботи не зроблено — **10 діб** |

Система відпрацювала: тріаж запустився з першої спроби, класифікував швидко.
Не спрацювала **конструкція**, і в двох місцях одразу:

1. Коментар тріажу не містив рішення-запиту — питання названо в прозі
   («варто підняти окремо»), без варіантів, наслідків, оборотності й треку.
   Формулювати рішення довелося yurii самому.
2. Коли yurii його сформулював — **не існувало кроку, який перетворює
   відповідь на роботу**. Відповідь пролежала в issue десять діб.

Це той самий клас, що `docs/16` §1 (deny-правила, обіцяні в документі й
відсутні в git): конструкція виглядає завершеною, поки не дійдеш до її кінця.

## 4. Фікс (`ai-fix.yml`) — що читати

Після `ai-approved` — за кілька хвилин або draft PR, або коментар
`ai-architectural` (фікс на півдорозі зрозумів, що план вимагає забороненого
шляху — заборонені шляхи `packages/db/src/migrations/**`, `infra/**`,
`.github/**`, `CLAUDE.md`, `docs/03_DECISIONS.md`,
`docs/12_TEMPLATE_CONTRACT.md`, `packages/db/src/schema.ts`,
`workers/cad/tests/snapshots/**` — ADR-035 §5.4). У другому випадку PR-у
немає — це очікувано, не помилка workflow'у.

Якщо є draft PR — читай його опис (план + «Як перевірити очима» +
«Опитування», якщо лишились варіанти) і переходь до §5.

## 5. Локальне рев'ю — одна команда

У СВІЖІЙ Claude Code-сесії (не та, що читала issue раніше — не обов'язково,
але рекомендовано для чистоти контексту):

```
Виконай docs/promts/ai-review-local.md для PR #<N>
```

Це запускає `agy`/Gemini як незалежного Рев'юера і Тест-інженера
(read-only), Claude верифікує й оформлює прийняті тест-кейси. Результат —
коментар на PR з вердиктом `agy` (дослівно, позначка «Gemini Reviewer
(unedited)») + підсумок від Claude. **Merge — після того, як прочитав ОБИДВА
вердикти** (Claude-Будівельник написав код; Gemini — незалежна перевірка;
розбіжність між ними — цінний сигнал, не привід ігнорувати один з них).

## 6. Конфлікт з Run 7 (Template Registry migration)

Поки триває Registry-міграція (трек T2, `docs/02_ROADMAP.md`,
`docs/promts/master-registry-track.md`) — файли студій/шаблонів
(`apps/web/src/components/*-{studio,editor,viewport}.tsx`,
`workers/cad/flatcraft_cad/templates/*.py`, `packages/types/src/templates/*.ts`)
активно переписуються по одному шаблону за раз (ADR-033). Будь-який ai-fix
у цій зоні ризикує зустрічними правками з Registry-PR того ж файлу.

**Правило:** поки T2 активний, тріаж ЗОБОВ'ЯЗАНИЙ класифікувати фікси у
перелічених нижче шляхах як `ARCHITECTURAL`, незалежно від того, наскільки
дрібним виглядає баг. Коментар тріажу має явно посилатись на це правило
(«Registry-міграція в процесі, `docs/17` §6») — так, дрібний typo-фікс теж
іде у звичайну чергу, а не через ai-fix.

**Точний перелік зони (станом на `d4c81be`, 2026-08-03).** Перевіряй за
фактичною структурою репозиторію, не за пам'яттю — після кожного
міграційного PR список per-template файлів коротшає:

| Шлях                                                                                         | Чому в зоні                                                                                                    |
| -------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| `apps/web/src/components/{z-bracket,wall-shelf,enclosed-shelf}-{studio,editor,viewport}.tsx` | 9 файлів трьох ще НЕ мігрованих шаблонів — саме їх переписує наступний PR Етапу 2                              |
| `apps/web/src/components/registry-template-{studio,editor,viewport}.tsx`                     | спільні компоненти, на які мігрують шаблони по одному                                                          |
| `apps/web/src/components/template-studio.tsx`                                                | точка входу, що маршрутизує між per-template і registry-компонентами                                           |
| `packages/templates/src/**`                                                                  | сам реєстр (`registry.ts`, `definition.ts`) і вже мігровані `perforated-panel/`, `corner-angle/`, `l-bracket/` |
| `workers/cad/flatcraft_cad/templates/*.py`                                                   | Python-паритет шаблонів + `registry.py`                                                                        |
| `workers/cad/tests/fixtures/ts_registry_slugs.json`                                          | згенерований фікстур slug-паритету TS↔Python — регенерується кожним міграційним PR                             |

**Тригер зняття (механічний, не «на око»):** завершення **Етапу 2** Run 7 —
міграція всіх шести шаблонів (`docs/promts/master-registry-track.md`), тобто
момент, коли `packages/templates/src/registry.ts` містить усі 6 slug'ів, а
per-template файли з першого рядка таблиці видалені. Станом на 2026-08-03
мігровано 3 з 6 (`perforated_panel`, `corner_angle`, `l_bracket`).

**Цей розділ — тимчасовий; його видалення — окремий пункт у #78.**

## 7. Правило «дані → issue»: digest і Sentry

`docs/14` §4.4 фіксує механізм самовдосконалення: **кожен пункт щотижневого
digest'а (Discord) і кожен новий Sentry-issue стає GitHub-issue АБО явно
позначається «accepted noise»** у digest-треді. Це джерело кандидатів для
`ai-fix` — не єдине (руками заведені баги теж ідуть тим самим шляхом), але
основне постачання. Заводь issue за формою (§2), навіть якщо джерело —
автоматичний digest-пункт: людський опис («Де / Кроки / Очікував / Побачив»)
важливий для тріажу, сирий Sentry-stacktrace чи цифра з digest'а — недостатньо.

## 8. Одноразове налаштування (перед першим використанням)

1. ✅ `claude setup-token` (локально) → скопіювати токен. _(Виконано;
   перевипущено 2026-07-31 — перший токен виявився недійсним, див. §9.)_
2. ✅ GitHub → Settings → Secrets and variables → Actions → New secret:
   `CLAUDE_CODE_OAUTH_TOKEN`. _(Виконано 2026-07-31.)_
3. ✅ Створити лейбли: `ai-plan-ready`, `ai-approved`, `ai-architectural`,
   `ai-need-info`. _(Виконано 2026-07-31 — крок випав із виконання на два
   тижні, бо жив лише тут і в description setup-PR; наслідок описано в §9,
   рядок «Прогін зелений, але лейбли issue не змінились».)_
4. ✅ **Acceptance-прогін**: взяти РЕАЛЬНИЙ дрібний баг зі свого списку,
   пройти повний цикл §1-§5 до merge, перевірити фікс очима на staging.
   _(Виконано 2026-08-03 на issue #84 → PR #97 → `d4c81be` → staging.)_

> **Правило (2026-07-31, урок цього setup'у):** кожен мануальний крок
> setup-run'у заноситься у закріплений issue **#78 «Черга yurii» У МОМЕНТ
> створення PR**, а не «потім». ADR і description PR — дублі для контексту,
> НЕ джерело істини (CLAUDE.md §0 п.5). Крок 3 цього ж списку випав саме
> тому, що жив лише тут і в description setup-PR: лейбли не існували, і
> перший зелений тріаж не зміг завершити перехід стану.

## 9. Типові фейли

| Симптом                                                                              | Причина                                                                                                                                                                                     | Дія                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Тріаж не запустився після `ai-fix`                                                   | Лейбл поставив користувач без write-доступу — GitHub це фізично не дозволяє (§1 gate)                                                                                                       | Не проблема цього флоу; постав лейбл сам                                                                                                                                                                                                                                                                                                                                                                               |
| `ai-fix.yml` впав на «Verify forbidden paths»                                        | Фікс зачепив заборонений шлях (permissions.deny не спрацював, backstop-скрипт впіймав)                                                                                                      | Прочитай лог job'у, видали/закрий PR і гілку вручну, заведи issue у звичайний трек                                                                                                                                                                                                                                                                                                                                     |
| Draft PR є, але тестів немає                                                         | Bug у промпті fix-стадії (регресійний тест мав бути обов'язковим)                                                                                                                           | Коментар на PR + `ai-review-local.md` все одно зловить це на кроці 2 (agy)                                                                                                                                                                                                                                                                                                                                             |
| `agy` переписав чужий файл поза `docs/promts/inputs/`                                | Відомий клас інциденту (2× Master Run 8) — `write_file(*)` ширший за задум                                                                                                                  | `git restore`, перевір byte-identical, продовж; рядок у `agy-stats.md`                                                                                                                                                                                                                                                                                                                                                 |
| Прогін зелений, але лейбли issue не змінились                                        | Термінального лейбла (`ai-plan-ready`/`ai-architectural`/`ai-need-info`) не існує в репо АБО `gh issue edit` впав — агент відпрацював, стан issue лишився старим                            | З 2026-07-31 ловиться кроком «Verify issue reached a terminal state» — job червоніє замість фальшивого success. Створити відсутній лейбл (§8 крок 3) і перезапустити тріаж                                                                                                                                                                                                                                             |
| `401 Invalid bearer token`, `num_turns: 1`, `total_cost_usd: 0`, миттєвий `is_error` | НЕдійсне ЗНАЧЕННЯ секрету `CLAUDE_CODE_OAUTH_TOKEN`. Це не модель, не версія дії, не квота і не «Pro-підписка непридатна в CI» — усі чотири гіпотези перевірені й спростовані 2026-07-19…31 | Перевипустити: `claude setup-token` з-під акаунта з підпискою → `printf '%s' '<token>' \| gh secret set CLAUDE_CODE_OAUTH_TOKEN --repo <owner/repo>` (через `printf`, щоб не занести хвостовий `\n` у значення)                                                                                                                                                                                                        |
| Будівельник у PR пише «локальний прогін тестів не виконано»                          | `ai-fix.yml` не має кроку `pnpm install` — у sandbox-середовищі дії немає `node_modules`, а install-команди впираються в permission-prompt, неможливий у headless                           | Очікувана поведінка, не збій. **Компенсуючий ручний контроль:** рев'юер ЗОБОВ'ЯЗАНИЙ сам перевірити «червоний до фікса» — відкотити фікс, лишивши тест, переконатись що падає, повернути (`docs/promts/ai-review-local.md`, крок 3-bis). Без цього культура 2.10.e формально не виконана: зелений тест на виправленому коді нічого не доводить. Прибрати ручний контроль можна лише разом із test-first proof job (A8) |
